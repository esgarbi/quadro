# Additional dependencies for the LLM examples (not required by quadro core).
#
# Install with:
#   pip install -r examples/microsoft_agent_framework/requirements.txt
#
# agent-framework is pre-1.0 for much of its history; pin to a tested range so
# a breaking upstream release does not silently break the LLM examples. Bump
# the upper bound deliberately after validating.

python-dotenv>=1.0
agent-framework>=1.0,<2.0
